  <header className={styles.navbar}>
    <div className={styles.container}>
      <Link to="/" className={styles.brand}>
        <span className={styles.logoBadge}>🧭</span>
        Egypt Tours Gate
      </Link>

      <button className={styles.burger} onClick={() => setMenuOpen(v => !v)} aria-label="Toggle menu">☰</button>

      <nav className={`${styles.menu} ${menuOpen ? 'open ' + styles.open : ''}`}>
        <ul className={styles.navList}>
          <li>
            <NavLink className={({isActive}) => isActive ? styles.linkActive : styles.link} to="/Home">Home</NavLink>
          </li>
          <li>
            <NavLink className={({isActive}) => isActive ? styles.linkActive : styles.link} to="#tours">Tours</NavLink>
          </li>

          {/* <li className={`${styles.dropdown} ${drop1Open ? 'open' : ''}`} onMouseEnter={() => setDrop1Open(true)} onMouseLeave={() => setDrop1Open(false)}>
            <button className={styles.dropToggle} onClick={() => setDrop1Open(v => !v)}>Destinations ▾</button> */}
             <li
              className={`${styles.dropdown} ${drop1Open ? 'open' : ''}`}
              {...(!isMobile && {
                onMouseEnter: () => setDrop1Open(true),
                onMouseLeave: () => setDrop1Open(false)
              })}
            >
              <button
                className={styles.dropToggle}
                onClick={isMobile ? toggleDrop1 : undefined}
              >
                Destinations ▾
              </button>
            <div className={styles.dropdownMenu}>
              <ul className={styles.dropdownList}>
                <li><NavLink className={styles.dropdownLink} to="#">Cairo</NavLink></li>
                <li><NavLink className={styles.dropdownLink} to="#">Luxor</NavLink></li>
                <li><NavLink className={styles.dropdownLink} to="#">Aswan</NavLink></li>
                <li><NavLink className={styles.dropdownLink} to="#">Hurghada</NavLink></li>
                <li><NavLink className={styles.dropdownLink} to="#">Sharm El Sheikh</NavLink></li>
              </ul>
            </div>
          </li>

          <li className={`${styles.dropdown} ${drop2Open ? 'open' : ''}`} onMouseEnter={() => setDrop2Open(true)} onMouseLeave={() => setDrop2Open(false)}>
            <button className={styles.dropToggle} onClick={() => setDrop2Open(v => !v)}>Experiences ▾</button>
            <div className={styles.dropdownMenu}>
              <ul className={styles.dropdownList}>
                <li><NavLink className={styles.dropdownLink} to="#">Nile Cruises</NavLink></li>
                <li><NavLink className={styles.dropdownLink} to="#">Pyramids & Sphinx</NavLink></li>
                <li><NavLink className={styles.dropdownLink} to="#">Desert Safari</NavLink></li>
                <li><NavLink className={styles.dropdownLink} to="#">Red Sea Diving</NavLink></li>
                <li><NavLink className={styles.dropdownLink} to="#">City Tours</NavLink></li>
              </ul>
            </div>
          </li>

          <li>
            <NavLink className={({isActive}) => isActive ? styles.linkActive : styles.link} to="#contact">Contact</NavLink>
          </li>
        </ul>
      </nav>

      <button className={styles.ctaBtn}>Book Now</button>
    </div>
  </header>